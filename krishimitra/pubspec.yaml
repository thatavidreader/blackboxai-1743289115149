name: krishimitra
description: Farm management Flutter application

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  intl: ^0.17.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test_coverage: ^0.5.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/